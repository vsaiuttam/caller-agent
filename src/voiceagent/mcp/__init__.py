"""Samvaad as an MCP client: the user's own apps, as tools for the agent.

Named for what it connects to. Inside this package `import mcp` still means
the MCP SDK — imports here are absolute — and the package itself is only
ever imported as `voiceagent.mcp`.
"""
